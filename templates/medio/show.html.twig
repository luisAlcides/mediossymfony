{% extends 'base.html.twig' %}

{% block title %}Detalles del Medio{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="text-center mb-4">Detalles del Medio</h1>

    <div class="card shadow-sm">
        <div class="card-body">
            <table class="table table-striped">
                <tbody>
                    <tr>
                        <th>ID</th>
                        <td>{{ medio.id }}</td>
                    </tr>
                    <tr>
                        <th>Nombre</th>
                        <td>{{ medio.nombre }}</td>
                    </tr>
                    <tr>
                        <th>Descripción</th>
                        <td>{{ medio.descripcion }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <div class="d-flex justify-content-between mt-4">
        <a href="{{ path('app_medio_index') }}" class="btn btn-secondary">
            <i class="bi bi-arrow-left"></i> Volver a la Lista
        </a>

        <a href="{{ path('app_medio_edit', {'id': medio.id}) }}" class="btn btn-warning">
            <i class="bi bi-pencil"></i> Editar
        </a>

        {{ include('medio/_delete_form.html.twig', { 'class': 'd-inline' }) }}
    </div>
</div>
{% endblock %}
