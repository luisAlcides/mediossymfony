{% extends 'base.html.twig' %}

{% block title %}Editar Medio{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="text-center mb-4">Editar Medio</h1>

    <div class="card shadow-sm">
        <div class="card-body">
            {{ include('medio/_form.html.twig', {'button_label': 'Actualizar'}) }}
        </div>
    </div>

    <div class="d-flex justify-content-between align-items-center mt-4">
        <a href="{{ path('app_medio_index') }}" class="btn btn-secondary">
            <i class="bi bi-arrow-left"></i> Volver a la lista
        </a>

        {{ include('medio/_delete_form.html.twig', { 'class': 'd-inline' }) }}
    </div>
</div>
{% endblock %}
